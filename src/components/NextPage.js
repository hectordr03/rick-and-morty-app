import React from 'react';

const NextPage = (props) => {
    const next = (props) => {
        this.setState ({

        })
    }

    const prev = () => {

    }

    return(
        <div>
            <h1>Next page component</h1>
        </div>
    )
}

export default NextPage;